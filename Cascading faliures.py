import scipy as sp
import numpy as np 
from generateSystem import returnSystem

def netMon(G,theta,a):
    S = 0
    for power in P:
        power = n/gen * power/np.mod(power)
    finished = False
    while (finished == False):
        

    return S

def dynamicCascade(n,m,P,a):
    S = 0

    return S/m

n=10
(gen,con,pas,A,P) = returnSystem(n)
print(A)